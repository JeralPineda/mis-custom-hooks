# useForm

Ejemplo de uso:

```
    const initialForm = {
        name: 'Jeral',
        email: 'jeral@jeral.com'

    };

    const  = [ formValues, handleInputChange, reset ] = useForm(initialForm);

```
